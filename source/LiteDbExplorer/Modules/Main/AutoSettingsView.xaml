<UserControl x:Class="LiteDbExplorer.Modules.Main.AutoSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:pt="http://propertytools.org/wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding DisplayName}" FontWeight="Bold" Margin="6,0,0,1" />
        <pt:PropertyGrid Grid.Row="1" SelectedObject="{Binding AutoGenContext}"
                         ShowReadOnlyProperties="False"
                         TabVisibility="VisibleIfMoreThanOne" 
                         Margin="0,0,8,0" ShowDeclaredOnly="True" />
    </Grid>
</UserControl>